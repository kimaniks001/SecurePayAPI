# securepay-control-centre

**Phase 1:** Scaffold only — no UI implementation.

Future operational administration interface. Must use administration APIs only — never direct database access. See [Control Centre Requirements](../../docs/operations/CONTROL_CENTRE_REQUIREMENTS.md).
